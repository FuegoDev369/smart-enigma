{
  "name": "smart-enigma",
  "version": "6.0.0",
  "private": true,
  "description": "Smart Enigma — client-side, offline encryption tool. Zero runtime dependencies.",
  "scripts": {
    "dev": "node build.mjs dev",
    "build": "node build.mjs build",
    "lint:css": "stylelint \"src/css/*.css\"",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "stylelint": "^17.14.0"
  }
}
